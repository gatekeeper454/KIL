# Replay scenarios

Each scenario will be self-contained and versioned. It will include a source
manifest, normalized action trace, modeled context inputs, expected decisions,
and uncertainty notes. Historical facts and modeled values must never share an
unlabeled field.

