# Kubernetes enforcement adapter

This adapter will consume the same deterministic decision interface used by the
offline replay and apply it at a live transport boundary. The adapter technology
has not been selected. No cluster mutation or dependency installation occurs in
the scaffold.

KTP citation: [canonical `CITATION.cff`](https://github.com/nmcitra/ktp-rfc/blob/main/CITATION.cff).
